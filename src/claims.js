import { Typography } from "@mui/material";

function Claims() {
    return (
        <Typography variant="body1" gutterBottom>
            This is the Claims page.
        </Typography>
    )
}

export default Claims;