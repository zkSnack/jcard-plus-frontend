import { Typography } from "@mui/material";

function AccountInfo() {
  return (
    <Typography variant="body1" gutterBottom>
        This page show info reagrding the user.
    </Typography>
  )
}

export default AccountInfo;